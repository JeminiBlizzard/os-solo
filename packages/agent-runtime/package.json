{
  "name": "@os-solo/agent-runtime",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.json --skipLibCheck || true"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.91.1",
    "@os-solo/api": "workspace:*",
    "@os-solo/db": "workspace:*",
    "@os-solo/shared": "workspace:*",
    "drizzle-orm": "^0.38.3"
  },
  "devDependencies": {
    "vitest": "^4.1.5"
  },
  "types": "dist/index.d.ts"
}
